{
  "name": "jogwheel-controller",
  "version": "1.0.0",
  "private": true,
  "description": "A dependency-free, multi-input jogwheel controller for the web",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check src/JogWheel.js && node --check examples/dual-dj/app.js && node --check examples/dual-dj/scratch-worklet.js && node --check server.js && node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
